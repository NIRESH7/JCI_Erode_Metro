# Production — copy to .env.production before Play Store release build
URL=https://api.jcierodegreencity.com

# Web OAuth client ID from Google Cloud Console (OAuth 2.0 Web client)
# Required for Google Sign-In on Android release builds.
GOOGLE_WEB_CLIENT_ID=your-web-client-id.apps.googleusercontent.com
